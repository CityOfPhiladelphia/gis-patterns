# Syntax highlighting
highlighter:         pygments

# Permalinks
#
# Use of `relative_permalinks` ensures post links from the index work properly.
permalink:           pretty
relative_permalinks: false

# Setup
title:               Phila.gov GIS Patterns
url:                 http://cityofphiladelphia.github.io/patterns
baseurl:             /patterns
destination:        _site
description:        The official GIS Pattern Portfolio for the City of Philadelphia
version:            0.1.0
# Assets
#
# We specify the directory for Jekyll so we can use @imports.
sass:
  sass_dir:          _scss
  style:            :compressed

collections:
  patterns:
    output: true
  templates:
    output: true
